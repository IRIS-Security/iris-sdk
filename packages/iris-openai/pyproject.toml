[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "iris-security-openai"
version = "0.1.0"
description = "IRIS governance for OpenAI — Cedar policy on every API call"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
authors = [{ name = "IRIS Platform", email = "sdk@iris.ai" }]
dependencies = [
  "iris-security-core>=0.1.0",
  "iris-security-sdk>=0.1.0",
]

[project.optional-dependencies]
openai = ["openai>=1.30"]
dev = [
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
  "ruff>=0.4",
]

[project.urls]
Homepage = "https://github.com/gimartinb/iris-sdk"
Repository = "https://github.com/gimartinb/iris-sdk"

[tool.setuptools.packages.find]
where = ["."]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
